package com.elec5619.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * OpenAPI Configuration for Swagger UI
 * Using default SpringDoc configuration to avoid potential issues
 */
@Configuration
public class OpenApiConfig {
    // Using default SpringDoc configuration
    // This will automatically generate OpenAPI documentation from controller annotations
}
